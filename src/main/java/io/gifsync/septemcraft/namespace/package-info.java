/**
 * The mod's namespace. Holds the id every registry name begins with, so that a feature can name what
 * it registers without reaching into the composition root that owns the entrypoint.
 */
@PackageKind(Kind.SHARED)
package io.gifsync.septemcraft.namespace;

import io.gifsync.septemcraft.structure.Kind;
import io.gifsync.septemcraft.structure.PackageKind;
